package com.example.inicial1.estadisticas;

import org.springframework.web.bind.annotation.CrossOrigin;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@CrossOrigin(origins = "*")
@RequestMapping(path = "api/estadisticas")
public class EstadisticasController {
    //Total ventas por cliente (puede ser en los ultimos meses o desde siempre)

    //Producto mas vendido

    //Producto menos vendido

    //Total facturado en cada mes

    //Total de ventas desde el inicio
}
